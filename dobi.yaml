# ===================================================
# alias
# ===================================================

alias=bb-gitversion-build:
  tasks:
  - gitversion-build
  annotations:
    description: "-> build all content"

alias=bb-gitversion-deploy:
  tasks: []
  annotations:
    description: "-> deploy all content"

alias=bb-gitversion-test: 
  tasks: 
  - gitversion-test
  annotations: 
    description: "-> test all content."